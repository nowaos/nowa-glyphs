# frozen_string_literal: true

source 'https://rubygems.org'

# SVG parsing and manipulation (scripts/lib/svg_tracker.rb)
gem 'nokogiri'

# Task runner — the Rakefile exposes every script under scripts/tasks/
gem 'rake'

group :test do
  gem 'minitest'
end
